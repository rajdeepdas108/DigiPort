---
layout: layouts/home.njk
fa-icon: 'fa-home'
eleventyNavigation:
  key: Home
  order: 1
  url: /#
---
<header class="w3-container w3-padding-32 w3-center w3-black header-3d" id="home">
  <h1 class="w3-jumbo">
    <span class="w3-hide-small">I'm</span> {{ metadata.title }}.
  </h1>
  <p>Innovator in Tech | Founder of BongBeast Infotech Pvt. Ltd. | Future-Ready Technologist</p>
  <img src="{{ 'img/w3images/r.jpg' | url }}" alt="boy" class="w3-image header-img" width="992" height="1108">
</header>

<style>
/* General 3D and animation styles */
body {
  font-family: 'Arial', sans-serif;
  background: linear-gradient(to bottom right, #1f1f1f, #343434);
  color: #ccc;
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

header {
  text-shadow: 0px 4px 8px rgba(0, 0, 0, 0.8);
}

.header-3d h1 {
  background: linear-gradient(45deg, #00c6ff, #0072ff);
  color: transparent;
  background-clip: text;
  -webkit-background-clip: text;
  font-weight: 700;
}

.header-img {
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
  border-radius: 20px;
  transition: transform 0.4s ease, box-shadow 0.4s ease;
}

.header-img:hover {
  transform: scale(1.05);
  box-shadow: 0 16px 30px rgba(0, 0, 0, 0.5);
}

.w3-text-light-grey {
  color: #e0e0e0 !important;
}

/* Skill Bars */
.w3-wide {
  font-weight: bold;
}

.w3-dark-grey {
  background: linear-gradient(to right, #4facfe, #00f2fe);
  box-shadow: inset 0px 2px 4px rgba(0, 0, 0, 0.5);
}

/* Pricing Cards */
.w3-ul {
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.4);
  transition: transform 0.3s ease-in-out;
}

.w3-ul:hover {
  transform: scale(1.05);
}

.w3-dark-grey.w3-xlarge {
  background: linear-gradient(to right, #ff6a00, #ee0979);
}

.w3-ul li {
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.w3-ul li:last-child {
  border: none;
}

button.w3-button.w3-white {
  background: linear-gradient(45deg, #ff9a9e, #fad0c4);
  border: none;
  padding: 12px 20px;
  font-size: 16px;
  transition: background 0.3s ease, transform 0.2s;
}

button.w3-button.w3-white:hover {
  background: linear-gradient(45deg, #ff758c, #ff7eb3);
  transform: translateY(-3px);
}

/* Testimonials */
img.w3-circle {
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.6);
}

.w3-margin-right {
  transition: transform 0.2s ease;
}

.w3-margin-right:hover {
  transform: scale(1.1);
}

/* Contact Section */
form {
  max-width: 600px;
  margin: auto;
  background: #2e2e2e;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
}

form input {
  border: none;
  padding: 10px;
  width: calc(100% - 22px);
  margin-bottom: 15px;
  background: #f5f5f5;
  border-radius: 5px;
}

form input:focus {
  outline: none;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

button[type="submit"] {
  width: 100%;
  background: linear-gradient(45deg, #ff512f, #dd2476);
  border: none;
  color: white;
  font-size: 18px;
  padding: 10px;
  border-radius: 5px;
  cursor: pointer;
}

button[type="submit"]:hover {
  background: linear-gradient(45deg, #dd2476, #ff512f);
  transform: scale(1.02);
}
</style>
